{
  "debug":       true,
  "development": true,
  "testing":     false,
  "csrf":        true,

  "database":   "sqlite:///development.db",
  "secret_key": "two can keep a secret key if one of them is dead"
}
